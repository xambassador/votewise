const base = "/api";

export const PING = `${base}/ping`;
export const ONBOARDING = `${base}/onboarding`;
export const USERNAME_AVAILABLE = `${base}/usernameAvailable`;

export default {
  PING,
  ONBOARDING,
  USERNAME_AVAILABLE,
};
