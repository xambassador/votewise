> I don't know machine learning. To do some experiments with coding agents, I vibe coded this service entirely.

# Setup

```bash
source ./.venv/bin/activate.fish
pip install -r requirements.txt
uv run main.py
```
