doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Password Changed
    style.
      body {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        margin: 0;
        padding: 0;
        background-color: hsla(214, 11.9%, 11.6%, 1); /* --black-500 */
        color: hsla(220, 14.3%, 95.9%, 1); /* --gray-100 */
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .email-wrapper {
        background-color: hsla(240, 5.3%, 11.2%, 1); /* --nobel-black-100 */
        border-radius: 8px;
        padding: 30px;
        margin-top: 20px;
        border: 1px solid hsla(240, 3.7%, 15.9%, 1); /* --nobel-black-200 */
      }
      .header {
        text-align: center;
        margin-bottom: 30px;
      }
      .logo {
        max-width: 120px;
        margin-bottom: 20px;
      }
      h1 {
        color: hsla(0, 0%, 89.8%, 1); /* --white-50 */
        font-size: 24px;
        font-weight: 600;
        margin-bottom: 16px;
      }
      p {
        color: hsla(218, 10.6%, 64.9%, 1); /* --gray-400 */
        font-size: 16px;
        line-height: 1.6;
        margin-bottom: 24px;
      }
      .info-box {
        background-color: hsla(240, 3.7%, 15.9%, 1); /* --nobel-black-200 */
        border-radius: 6px;
        padding: 16px;
        margin: 24px 0;
      }
      .info-box p {
        margin: 8px 0;
        font-size: 14px;
      }
      .info-box strong {
        color: hsla(0, 0%, 89.8%, 1); /* --white-50 */
      }
      .action-button {
        display: inline-block;
        background-color: hsla(18, 27%, 49.4%, 1); /* --orange-50 */
        color: hsla(0, 0%, 89.8%, 1); /* --white-50 */
        text-decoration: none;
        padding: 12px 24px;
        border-radius: 6px;
        font-weight: 500;
        margin: 16px 0;
        text-align: center;
      }
      .action-button:hover {
        background-color: hsla(18, 27%, 45%, 1); /* slightly darker orange */
      }
      .warning-text {
        color: hsla(0, 62%, 55%, 1); /* red warning color */
        font-size: 14px;
        background-color: hsla(0, 62%, 55%, 0.1);
        padding: 12px 16px;
        border-radius: 6px;
        margin: 24px 0;
      }
      .divider {
        height: 1px;
        background-color: hsla(216, 12.2%, 83.9%, 0.1); /* --gray-300 with opacity */
        margin: 30px 0;
      }
      .footer {
        text-align: center;
        color: hsla(220, 8.9%, 46.1%, 1); /* --gray-500 */
        font-size: 14px;
        margin-top: 30px;
      }
      .footer p {
        color: hsla(220, 8.9%, 46.1%, 1); /* --gray-500 */
        font-size: 14px;
        margin: 5px 0;
      }
      .help-text {
        font-size: 14px;
        color: hsla(220, 8.9%, 46.1%, 1); /* --gray-500 */
      }
      @media only screen and (max-width: 600px) {
        .container {
          padding: 10px;
        }
        .email-wrapper {
          padding: 20px;
        }
      }

  body
    div.container
      div.email-wrapper
        div.header
          if logo
            img.logo(src=logo, alt="App Logo")
          h1 Password Changed Successfully

        p Hi #{firstName},
        p Your password was successfully changed on #{changedAt}. You can now use your new password to sign in to your account.

        div.info-box
          p
            strong Device: #{" "}
            | #{device || 'Unknown device'}
          p
            strong Location: #{" "}
            | #{location || 'Unknown location'}
          p
            strong IP Address: #{" "}
            | #{ipAddress || 'Unknown'}

        p.warning-text If you did not make this change, please reset your password immediately and contact our support team.

        div(style="text-align: center;")
          a.action-button(href=resetLink) Reset Password

        div.divider

        p.help-text If you made this change, you can safely ignore this email. This notification was sent to keep your account secure.

        div.footer
          p If you have any questions, contact our support team at support@votewise.com
          p &copy; #{new Date().getFullYear()} Votewise. All rights reserved.
