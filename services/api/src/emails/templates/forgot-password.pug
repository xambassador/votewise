doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Password Reset
    style.
      body {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        margin: 0;
        padding: 0;
        background-color: hsla(214, 11.9%, 11.6%, 1); /* --black-500 */
        color: hsla(220, 14.3%, 95.9%, 1); /* --gray-100 */
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .email-wrapper {
        background-color: hsla(240, 5.3%, 11.2%, 1); /* --nobel-black-100 */
        border-radius: 8px;
        padding: 30px;
        margin-top: 20px;
        border: 1px solid hsla(240, 3.7%, 15.9%, 1); /* --nobel-black-200 */
      }
      .header {
        text-align: center;
        margin-bottom: 30px;
      }
      .logo {
        max-width: 120px;
        margin-bottom: 20px;
      }
      h1 {
        color: hsla(0, 0%, 89.8%, 1); /* --white-50 */
        font-size: 24px;
        font-weight: 600;
        margin-bottom: 16px;
      }
      p {
        color: hsla(218, 10.6%, 64.9%, 1); /* --gray-400 */
        font-size: 16px;
        line-height: 1.6;
        margin-bottom: 24px;
      }
      .reset-button {
        display: inline-block;
        background-color: hsla(18, 27%, 49.4%, 1); /* --orange-50 */
        color: hsla(0, 0%, 89.8%, 1); /* --white-50 */
        text-decoration: none;
        padding: 12px 24px;
        border-radius: 6px;
        font-weight: 500;
        margin: 16px 0;
        text-align: center;
      }
      .reset-button:hover {
        background-color: hsla(18, 27%, 45%, 1); /* slightly darker orange */
      }
      .divider {
        height: 1px;
        background-color: hsla(216, 12.2%, 83.9%, 0.1); /* --gray-300 with opacity */
        margin: 30px 0;
      }
      .footer {
        text-align: center;
        color: hsla(220, 8.9%, 46.1%, 1); /* --gray-500 */
        font-size: 14px;
        margin-top: 30px;
      }
      .footer p {
        color: hsla(220, 8.9%, 46.1%, 1); /* --gray-500 */
        font-size: 14px;
        margin: 5px 0;
      }
      .help-text {
        font-size: 14px;
        color: hsla(220, 8.9%, 46.1%, 1); /* --gray-500 */
      }
      @media only screen and (max-width: 600px) {
        .container {
          padding: 10px;
        }
        .email-wrapper {
          padding: 20px;
        }
      }

  body
    div.container
      div.email-wrapper
        div.header
          if logo
            img.logo(src=logo, alt="App Logo")
          h1 Reset Your Password

        p Hi #{firstName},
        p We received a request to reset your password. Use the button below to set a new password for your account:
        div(style="text-align: center;")
          a.reset-button(href=resetLink) Reset Password

        p If you didn't request a password reset, you can safely ignore this email.
        p.help-text This password reset link will expire in #{expiresIn} #{expiresInUnit}.
        div.divider
        p.help-text If you're having trouble clicking the "Reset Password" button, copy and paste the URL below into your web browser:
        p.help-text
          a(href=resetLink, style="color: hsla(218, 10.6%, 64.9%, 1); word-break: break-all;") #{resetLink}
        div.footer
          p If you have any questions, contact our support team at support@votewise.com
          p &copy; #{new Date().getFullYear()} Votewise. All rights reserved.
